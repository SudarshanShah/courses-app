services:
  mongodb:
    image: 'mongo:latest'
    environment:
      - 'MONGO_INITDB_DATABASE=mydb'
      - 'MONGO_INITDB_ROOT_PASSWORD=1234'
      - 'MONGO_INITDB_ROOT_USERNAME=mongo'
    ports:
      - '27017:27017'
