spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: mydb
      
server:
  port: 8081      
      
logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG      